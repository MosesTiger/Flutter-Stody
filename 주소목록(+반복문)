ListView.builder(
  itemCount: 3,
  itemBuilder: (context, i) {
    print(i);
    return ListTile(
      leading : Image.asset('profile.png'),
      title : Text('홍길동'),
    )
  }
);  
# 원하는 만큼 리스트를 만들 수 있다. 
